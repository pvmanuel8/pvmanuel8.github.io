import Portfolio from "./src/Portfolio";

/**
 * Re-exportación del componente principal Portfolio desde la arquitectura modular `src/`.
 * Esto permite importar `<Portfolio />` de forma limpia tanto si se usa la raíz como si se utiliza la carpeta `src/`.
 */
export default Portfolio;
